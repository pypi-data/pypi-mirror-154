# Extensions needed to check files.
EXT_CHECK = (".msi", ".vsix", ".zip")
# Extensions needed to unpack.
EXT_UNPACK = (".msi", ".vsix", ".zip", ".cab")
# Length of lines which delimit elements in output.
OUT_DELIM_LEN = 80
